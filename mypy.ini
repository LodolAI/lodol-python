[mypy]
# Full strict type checking, matching the main lodol repository's policy.
#
# python_version is intentionally left unset: mypy checks against the
# interpreter it runs under (Python 3.13 in CI). Runtime support across the
# declared requires-python >= 3.9 range is verified separately by the pytest
# matrix in .github/workflows/test.yml.
strict = True
